# Copy this file to .env and fill in real values. Never commit the real .env file.

# Required — signs session tokens. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
TOKEN_SECRET=

# Required for the AI document review endpoint to work. Get one for free
# (no card needed) from https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# Optional — which Gemini model reviews documents.
GEMINI_MODEL=gemini-3.1-flash-lite

# Seeds the first admin account on first run (only used if no admin exists yet).
ADMIN_EMAIL=admin@anyvisa.co.uk
ADMIN_PASSWORD=changeme-immediately

# Set this to your ngrok URL (or whatever public URL points at this
# machine) once you have it — e.g. https://your-name.ngrok-free.app
# Needed for cookies/CORS to work correctly from the real site.
ALLOWED_ORIGIN=*

# Port the server listens on.
PORT=3000

# Where the SQLite database file is stored — a real file on this computer's
# real disk, so it survives restarts. Defaults to ./data/anyvisa.db
# DB_PATH=./data/anyvisa.db
